package OnlineStoreApp.demo.model;

import org.junit.Test;

import java.math.BigDecimal;

import static org.hamcrest.MatcherAssert.assertThat;

public class ShoppingCartTests {

    @Test
    public void should_add_an_item_to_a_cart(){

       /* ShoppingCart cart = new ShoppingCart();
        cart.addProduct(new Product(1, "flowers", "tulip", BigDecimal.valueOf(10), 15));

      int totalProductCount= cart.totalNumberOfProducts();
      assertThat(totalProductCount).isEqualTo(1);*/

    }

}
